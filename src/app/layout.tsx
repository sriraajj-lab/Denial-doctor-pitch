import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Denial Doctor - AI-Powered Healthcare RCM Platform',
  description: 'Stop Leaving Revenue on the Table. Healthcare claim denials cost providers $262 billion annually. Denial Doctor AI recovers what\'s yours.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
